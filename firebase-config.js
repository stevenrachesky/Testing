// Fill this in with YOUR Firebase project's web app config.
// (Project settings -> General -> "Your apps" -> SDK setup and configuration -> Config)
// These values are safe to commit — they are public client identifiers, not secrets.
// Access to your data is controlled by the Firestore security rules described in README.md.
//
// Until you replace YOUR_API_KEY_HERE below, the app automatically falls back to
// storing answers only in this browser (localStorage) so you can try it out locally
// before setting up Firebase.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY_HERE",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
