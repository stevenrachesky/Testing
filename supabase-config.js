// Fill this in with YOUR Supabase project's URL and anon (public) key.
// (Project -> Project Settings -> API)
// The anon key is safe to commit — it's a public client identifier, not a secret.
// Access to your data is controlled by the Row Level Security policies described
// in README.md, not by keeping this key hidden.
//
// Until you replace the placeholders below, the app automatically falls back to
// storing answers only in this browser (localStorage) so you can try it out locally
// before setting up Supabase.
export const supabaseConfig = {
  url: "https://YOUR_PROJECT_REF.supabase.co",
  anonKey: "YOUR_ANON_KEY",
};
